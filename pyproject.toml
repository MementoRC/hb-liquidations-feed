[project]
name = "hb-liquidations-feed"
dynamic = ["version"]
description = "A modular liquidations data feed for cryptocurrency exchanges"
requires-python = ">=3.10"
license = {text = "Apache-2.0"}
dependencies = [
    "aiohttp>=3.8.0",
    "pandas>=1.0.0",
    "pydantic>=2",
]

[build-system]
requires = ["hatchling>=1.18.0"]
build-backend = "hatchling.build"

[tool.hatch.version]
path = "liquidations_feed/__about__.py"

[tool.hatch.build.targets.wheel]
packages = ["liquidations_feed"]

[tool.pixi.workspace]
channels = ["conda-forge"]
platforms = ["linux-64", "osx-arm64"]

[tool.pixi.dependencies]
python = ">=3.10,<3.13"
aiohttp = ">=3.8.0"
pandas = ">=1.0.0"
pydantic = ">=2"

[tool.pixi.feature.dev.dependencies]
pytest = ">=7.4.0"
pytest-asyncio = ">=0.21.0"
pytest-cov = "*"
pytest-mock = "*"
pytest-xdist = ">=3.0.0"
ruff = "*"
mypy = "*"

[tool.pixi.environments]
default = { features = ["dev"], solve-group = "default" }

[tool.pixi.tasks]
test = "pytest tests/"
test-unit = "pytest tests/unit/"
lint = "ruff check liquidations_feed tests"
format = "ruff format liquidations_feed tests"
format-check = "ruff format --check liquidations_feed tests"
quality = { depends-on = ["lint", "format-check"] }
check = { depends-on = ["quality", "test"] }

[tool.ruff]
target-version = "py310"
line-length = 100

[tool.ruff.lint]
select = ["E", "F", "I", "W", "B", "UP", "N", "SIM"]

[tool.pytest.ini_options]
asyncio_mode = "auto"
testpaths = ["tests"]

[tool.hummingbot.supersedes]
test_paths = ["test/hummingbot/data_feed/liquidations_feed"]
exchanges = ["binance"]
